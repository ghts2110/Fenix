// const login = require('../components/**/*.js');

test.concurrent('performance test login non existing account', async () => {
    expect(3).toBe(3);
});

test.concurrent('performance test login existing account', async () => {
    expect(3).toBe(3);
});

test.concurrent('performance test login missing email', async () => {
    expect(3).toBe(3);
});

test.concurrent('performance test login missing password', async () => {
    expect(3).toBe(3);
});

test.concurrent('performance test login password is space', async () => {
    expect(3).toBe(3);
});

test.concurrent('performance test login email is space', async () => {
    expect(3).toBe(3);
});

test.concurrent('performance test login and password missing', async () => {
    expect(3).toBe(3);
});

test.concurrent('performance test missing password button', async () => {
    expect(3).toBe(3);
});

test.concurrent('performance test email and password is space', async () => {
    expect(3).toBe(3);
});

test.concurrent('performance test correct email wrong password', async () => {
    expect(3).toBe(3);
});

test.concurrent('performance test correct password wrong email', async () => {
    expect(3).toBe(3);
});

test.concurrent('performance test remember me check box', async () => {
    expect(3).toBe(3);
});

test.concurrent('performance test invalid characters', async () => {
    expect(3).toBe(3);
});