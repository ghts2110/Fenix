// const login = require('../components/**/*.js');

test.concurrent('performance test missing email', async () => {
    expect(3).toBe(3);
});

test.concurrent('performance test not exist email', async () => {
    expect(3).toBe(3);
});

test.concurrent('performance test wright email', async () => {
    expect(3).toBe(3);
});

test.concurrent('performance test wright email sending email', async () => {
    expect(3).toBe(3);
});