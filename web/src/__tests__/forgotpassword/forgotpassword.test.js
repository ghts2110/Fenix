// const login = require('../components/**/*.js');

test('test missing email', () => {
    expect(3).toBe(3);
});

test('test not exist email', () => {
    expect(3).toBe(3);
});

test('test wright email', () => {
    expect(3).toBe(3);
});

test('test wright email sending email', () => {
    expect(3).toBe(3);
});
